#!/bin/sh

old="$(flavours current)"
flavours list -l | fzf --preview='flavours apply -l {}' || \
  (flavours apply "${old}" && disable-window-shadow) && \
  (flavours apply "$(flavours current)" && disable-window-shadow)
