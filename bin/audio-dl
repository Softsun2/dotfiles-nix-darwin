#!/bin/sh

# $1 -- the audio url
yt-dlp \
  --extract-audio \
  --audio-format mp3 \
  --xattrs \
  --embed-thumbnail \
  --no-abort-on-error \
  --output '~/Music/my-music/%(uploader)s/%(album)s/%(title)s.%(ext)s' \
  $1
